import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Austin-TeeTime-Optimizer-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
